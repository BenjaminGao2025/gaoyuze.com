# 写作说明

## 先读：草稿隐私

此仓库目前公开。`draft: true` 只表示不生成网页，不代表 GitHub 文件保密。Git 历史、上传到 `public/uploads` 的图片，以及预览部署也可能公开。不要把家人资料、账户资料、未授权客户信息、密钥或私人草稿提交到公开仓库。更改仓库可见性需要站主另行决定；本次重构没有改权限。

在开始保存私人草稿前：把内容放在本地/私有位置，或将仓库改为私有并重新验证 Cloudflare、Pages CMS 的最小授权。已经公开过的内容不能靠切换私有保证收回。

## 浏览器写作（需站主首次授权）

1. 打开 https://app.pagescms.org ，使用 GitHub 登录。
2. 仅授权 `BenjaminGao2025/gaoyuze.com`。不要授权所有仓库。选择要编辑的分支；重构验收期间不要编辑 main。
3. 打开“新文章”。填写标题、网址标识和日期；语言默认中文；正文可以直接写，图片走上传区。
4. “草稿”默认开启。保存会写入 GitHub，因此不是隐私开关。
5. 在编辑器内检查文字与格式。编辑器预览不等于网站最终排版。
6. 确认公开内容后关闭“草稿”，保存。只有构建/部署成功后才算上线；构建失败时网站保留此前版本。
7. 上线后检查正式文章链接。修订使用同一文件和同一网址标识。撤回时重新开启“草稿”，等待重新部署，但历史版本/缓存/旧预览不保证消失。

`.pages.yml` 已提供配置，不代表 GitHub 登录授权或网页编辑器端到端测试已经完成。

新文使用 `.md`。既有 `.mdx` 文件保留原文与地址，不交给富文本编辑器改写，以免损坏 MDX。需要编辑旧文时先做分支备份和格式回归测试。

## Markdown 文件方式

将文件保存到 `src/content/articles/your-slug.zh.md`：

```markdown
---
title: 文章标题
pageSlug: your-slug
lang: zh
date: 2026-09-07
summary: 一两句简介，也可以留空。
tags: [学习]
draft: true
---

从正文开始写。
```

缺少 `draft` 时按草稿处理。只有显式 `draft: false` 才公开。日期仅用于排序和显示，不是定时发布开关。`pageSlug` 发布后不更改。翻译版使用相同 `translationKey`、不同 `lang`。

## 发布前检查

核对事实与引用；清除个人隐私；检查图片授权与替代文字；检查手机排版和链接；确认愿意公开，再关闭草稿。AI 可协助整理，不应自动决定发布。

## 首次后台验收（必须人工完成）

用不含私人信息的测试文章验证：新建、中文输入、保存草稿、插入图片、列表、代码块、表格、修改、关闭草稿发布、重新开启草稿撤回。图片替代文字必须检查。暂不承诺富文本编辑器完整保留复杂 Markdown/MDX；特殊格式使用源码模式并回读文件。

## 备份与回退

文章、图片、配置、依赖锁文件一起备份。GitHub 不是唯一备份：定期将整个仓库（含历史）复制到另一位置，并实际演练恢复。代码回退使用 revert，不重写 main 历史。已发布文章地址尽量保留；删除与重命名前先安排重定向。
