# 视觉与阅读设计

## 方向

安静的个人刊物，不做产品发布页。暖纸底色、深墨绿文字、少量赭红；宋体标题与系统无衬线正文搭配，英文长文使用 Georgia。无远程字体、无动画、无 UI 组件库、无新依赖。

首页：印记式站标、紧凑介绍、按日期排列的文章，不强制封面图；文章数来自已发布内容。文章正文和发布状态不因 UI 调整改变。

阅读：正文最大 44rem，默认桌面 19px / 手机 18px，随浏览器文字大小缩放。桌面宽屏侧边目录；窄屏原生 details 折叠目录。长文使用严格中文换行；代码采用浅色语法高亮，宽代码和表格在自身区域滚动。继续阅读只指向已发布文章，优先当前语言。打印隐藏导航与目录。普通文章仍然没有客户端脚本。

## 约束

- 不改已有正文，不清除用户测试文章，不改 Pages CMS、权限或生产分支。
- 不通过截断标题、裁掉正文或隐藏页面溢出来假装修好了手机排版。
- 不隐藏已发表的英文文章；翻译链接保留。
- 不强行把未知的个人经历写入关于页；只复用已经确定的网站定位。
- 字体使用本地系统回退，Mac、Windows、Android 的字形会有差别。
- 暂无手动深色模式，也没有滚动追踪或阅读时长监控。

## 测试

保留构建、原有网址/内部链接、草稿、搜索与键盘回归。在新增 browser/reading.spec.ts 中覆盖 320 / 390 / 768 / 1024 / 1440px 页面、目录锚点、200% 文字与间距覆盖、关闭 JavaScript 阅读和打印，以及主文字色的对比度。覆盖这些项目不等于宣称完整 WCAG 合规或已测试所有浏览器。

CI 产物 site-review 的 qa/editorial/ 保存真实页面桌面和手机截图；最终目视验收以对应提交的 Cloudflare 预览为准。
